# 隐私说明 / Privacy

本程序会把「歌曲编号 → 曲名」的对照关系分享给社区，**但只在你明确同意之后**，并且**只上传曲名与 3 段音频指纹**（用于识别同一首曲子）。

## 一、默认什么都不上传

- 安装后默认状态是未同意上传。
- 试听台开始前会弹一次询问：是否愿意把你起的曲名分享给社区，让其他人不用再自己分辨？
  可选「愿意」「不愿意」「以后再说」。选不愿意就永远不传，其余功能照常。
- 未同意时不会有任何上传请求（拉取公开曲目名单除外）。

## 二、会下载什么

从本仓库 data/catalog.json 拉取社区曲目名单（编号 → 曲名）。纯公开数据，不含个人信息，只读。

## 三、同意后会可能上传什么

| 字段 | 例子 | 说明 |
| --- | --- | --- |
| key | midi_8815_1786281418 | 曲库文件夹名，本身就是「上传批次编号 + 时间戳」，不含身份信息 |
| name | 千本樱 | 你起的曲名 |
| hash | a1b2c3… | key + name 的 SHA-256 前 16 位，用于去重校验 |
| fps | ["<512 字符>","<512 字符>","<512 字符>"] | 3 段音频指纹（第 20s / 80s / 140s 起各 60 秒）。作用：同一首歌在不同用户机器上文件夹编号完全不同，只有指纹能认出"这是同一首"，从而让没听过的用户自动命名。是**单向派生数据**，还原不出音频 |

## 三·补充、关于音频指纹

指纹是量化色度特征（64 帧 × 12 维 × 4 比特），属于**单向派生数据**：
它**无法还原成音频**，也无法用来播放，只用于判断"是不是同一首曲子"。
它不包含任何个人信息。

## 四、绝对不上传

- 用户名、昵称、UID、账号、头像、邮箱
- 设备 ID、设备型号、主板/显卡信息、IP、机器名、Windows 用户名
- 任何绝对路径（C:\Users\xxx\…、E:\…）——路径不会离开你的电脑
- 音频、视频、封面图、歌词文件
- 聊天记录、信件、记忆（memory）内容
- 数据库文件本身

## 五、上传怎么发生

1. 你在试听台起名 → 名字先只写进你自己的数据库
2. 同意分享时，累积到本地队列（community-upload-queue.json）
3. 你点「提交到社区」后，用你的 GitHub 账号向本仓库提一个 PR（data/inbox/ 下新增一个小文件）
4. 维护者合并后，所有用户的名单才更新

不上传数据库、不上传你的曲库清单、不上传你还没命名的歌。

## 六、可以反悔

- 设置里随时关掉分享开关；可清空待上传队列
- 已提交到 GitHub 的曲名属于公开数据，无法撤回——第一次同意前请想清楚

## 七、本地数据位置

| 内容 | 位置 |
| --- | --- |
| 数据库备份（每次会话写名前自动备份一次） | <OliviaSoul 数据目录>\database\backup-listen-<时间戳>.sqlite |
| 试听片段缓存 | <OliviaSoul 数据目录>\listen-clips\ |
| 命名日志 | <程序目录>\记录\试听命名.csv |
| 待上传队列 | <OliviaSoul 数据目录>\community-upload-queue.json |

只在你本机，不会被上传。想清理直接删目录。