# 开源软件声明 / Third-Party Notices

本程序使用了以下开源软件与第三方组件。我们在此致谢，并说明各自的许可证与合规情况。

## 一、随程序分发的组件

| 组件 | 版本 / 来源 | 许可证 | 许可证文本位置（程序目录内） |
| --- | --- | --- | --- |
| **Node.js 运行时** | 22.22.0 | MIT | `runtime\NODE-LICENSE.txt` |
| **FFmpeg / FFprobe** | n8.1（**LGPL 构建**） | LGPL v3 | `runtime\ffmpeg\LICENSE.txt` |
| **whisper.cpp + ggml 模型** | v1.9.2 | MIT | `runtime\whisper\LICENSE.txt` |
| **Microsoft Edge WebView2 SDK** | 见工程引用 | Microsoft 软件许可条款 | `licenses\WebView2\LICENSE.txt`、`NOTICE.txt` |
| **midi-file**（唯一的 npm 依赖） | 1.2.4 | MIT | 随 `app\node_modules\midi-file\LICENSE.md` |

以上组件的许可证文本**均由打包流程自动随程序分发**，未做任何修改。

### 关于 FFmpeg（LGPL）

本程序分发的是 FFmpeg 的 **LGPL 构建**（非 GPL 构建），以动态/独立可执行文件形式调用，
未与程序代码静态链接，符合 LGPL 的再分发要求。若你需要 FFmpeg 源码，请见
`installer\SOURCE.md` 中记录的获取方式，或访问 https://ffmpeg.org/ 。

## 二、仅在打包阶段使用的工具（不随程序分发）

| 工具 | 许可证 | 说明 |
| --- | --- | --- |
| Inno Setup 6 | 允许非商业使用 | 仅用于生成安装包，不随程序分发 |
| .NET SDK | MIT | 仅用于编译原生宿主 |
| .NET Framework 4.6.2 | 随 Windows 提供 | 原生宿主的目标框架，用户无需安装 |

## 三、本项目代码的授权状况（**请务必阅读**）

### 1. 上游代码

本分支基于上游开源项目 **coderscsy/linli**（其自身又基于 **yilangren/OliviaSoul**）。

**上游仓库当前未声明任何开源许可证**（仓库内没有 LICENSE / COPYING 文件）。
按著作权法默认规则，未声明许可证意味着**保留所有权利**。因此：

- 本分支**不代表**已获得上游的再分发授权；
- 本分支的公开分发已获上游作者许可；
- 更稳妥的替代做法是：只公开**本分支新增的代码差异**，由使用者自行与上游代码组合。

本声明如实记录这一状况，供使用者自行判断与决策。

### 2. 本分支新增的代码

除上游代码外，本分支新增的部分（试听起名、画面识别、社区曲目名单、依赖自检、
问题反馈、社区名单门禁与指纹工具等）由本分支作者编写，采用 **MIT 许可证**发布，
详见 `新增代码许可证.md`。该授权**仅覆盖新增部分**，不改变上游代码的授权状态。

## 四、内容与素材

本程序**不分发任何游戏素材、音视频、图片、字体或歌词文件**。
程序界面使用的图标与图片来自上游项目。曲目名称与音频指纹由用户自愿分享，
不含任何受版权保护的媒体内容。详见 `免责声明.md`。